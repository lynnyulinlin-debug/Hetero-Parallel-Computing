# 6.1 SIMD 概念

- **SIMD（Single Instruction Multiple Data）**：一条指令处理多个数据元素。
- 与多线程的区别：SIMD 是核内并行，与多核并行正交、可叠加。
- 〔图占位：标量 vs 向量寄存器的数据处理对比〕

### 6.1.1 为什么 SIMD 常被低估

很多 CPU 程序即使已经多线程化，性能仍然会被单核内部的标量执行拖住。  
这时候 SIMD 的价值就体现出来了：它不增加线程数量，也不改变任务分工，而是把“单次计算能同时处理多少数据”这件事抬高。

