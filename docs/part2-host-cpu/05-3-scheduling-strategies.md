# 5.3 调度策略

- `static` vs `dynamic` vs `guided`：负载均衡 vs 调度开销的权衡。
- 〔图占位：三种调度在不均衡负载下的性能对比〕

### 5.3.1 调度策略其实是在回答“谁来切分工作”

如果任务非常均匀，`static` 最省开销；如果任务很不均匀，`dynamic` 或 `guided` 更稳。  
这意味着调度策略不是“语法选项”，而是在替你决定：

- 工作切得多细；
- 线程多久去拿一次新任务；
- 负载不均时，是宁愿多一点调度开销，还是宁愿少一点同步。

