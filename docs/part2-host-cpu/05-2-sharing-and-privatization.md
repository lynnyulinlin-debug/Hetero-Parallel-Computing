# 5.2 数据共享与私有化

- `shared` / `private` / `firstprivate` / `reduction` 子句。
- 归约子句如何安全地合并各线程的部分结果（对照第3章 Reduce）。

### 5.2.1 共享变量为什么最容易出错

OpenMP 最容易出问题的地方，不是语法，而是变量归属关系：

- 这个变量是不是每个线程都能改？
- 这个变量是只读共享，还是线程私有？
- 中间结果应该在本地累积，还是直接写回全局？

如果这些边界没想清楚，就会出现竞态、错误归约或难以复现的结果。

