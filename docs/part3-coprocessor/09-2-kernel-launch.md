# 9.3 核函数编写与启动（`<<<>>>` 语法）

- `__global__` 修饰、`kernel<<<blocks, threads>>>(...)` 启动配置。
- 边界检查的必要性（线程数向上取整）。
- 对照代码：`examples/ch09_vec_add/vec_add_cuda.cu`。

## 9.3.1 本章示例映射

| 示例 | 你要看什么 | 结论 |
|------|------------|------|
| `examples/ch09_vec_add/` | H2D/D2H 传输、核函数启动、带宽瓶颈 | 向量加法是典型带宽受限任务，GPU 的优势主要来自更高带宽 |
| `examples/ch09_reduce/` | 归约依赖、共享内存、同步、bank 冲突 | 归约的核心难点是如何减少同步和全局原子操作 |
| 第9.6节矩阵乘 | 分块平铺、共享内存复用 | 矩阵乘是计算受限的正面教材，用来和前两个例子形成对照 |

