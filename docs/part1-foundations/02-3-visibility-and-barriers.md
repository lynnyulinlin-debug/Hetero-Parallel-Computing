# 2.3 原子操作与内存屏障：让数据可见

“写入已经发生”不等于“其他核心已经看见”。这正是并发里最容易误判的地方。

- CPU 有缓存一致性，但一致性不等于立即可见。
- 原子操作保证操作本身不可分割，减少读写交错带来的错误。
- 内存屏障和内存序决定读写对其他线程的可见顺序。

这一节只建立直觉：并发正确性依赖“看得见”和“按顺序看见”。不用在这里深挖内存模型细节，但要记住它们会直接影响第2.4节的队列安全、第4章的共享工作队列，以及后面异构系统里主机和设备之间的状态同步。
