# 2.1 进程 vs 线程：并行的基本单位

进程和线程的区别，决定了你是把任务切到“隔离的容器”里，还是切到“共享地址空间的执行流”里。

- 进程天然隔离，适合边界清晰、需要安全隔离的场景。
- 线程共享地址空间，适合频繁交换数据、需要低开销协作的场景。
- 进程切换和跨进程通信通常更重，线程切换和共享内存通常更轻，但也更容易引入竞态。
- 〔图占位：进程与线程的内存布局对比〕

这一节的重点不是背定义，而是先建立一个判断：当任务之间要频繁共享中间结果时，线程往往比进程更自然；当任务边界更强、失败隔离更重要时，进程可能更合适。第4章会继续把这个判断落到线程池和 CPU 并发实现里。
