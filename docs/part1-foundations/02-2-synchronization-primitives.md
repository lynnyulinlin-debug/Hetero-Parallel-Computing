# 2.2 线程同步原语：让队列安全运行

线程一旦共享数据，就不能只靠“约定”保证正确性，必须把访问规则写进同步原语里。

- 互斥锁适合保护临界区，让同一时刻只有一个线程修改共享状态。
- 读写锁适合读多写少的场景，允许多个读者并发，但写者要独占。
- 条件变量适合“等条件满足再继续”，典型就是队列空/满时的等待与唤醒。

这一节想传达的不是 API 列表，而是成本意识：同步能让并发正确，但也会带来阻塞、唤醒和切换开销。后面第4章的线程池、第5章的 OpenMP 归约、第7章的 Host/Device 交接，都会反复遇到这个问题。
